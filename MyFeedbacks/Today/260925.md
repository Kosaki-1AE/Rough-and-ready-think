#Mine研究/実装 
$$\text{g}\frac{DB}{Dt}=R_{\text{self}}+R_\text{env}$$$$\frac{DB}{Dt}=\frac{\partial\text{B}}{\partial t}+(\text{B}\cdot\nabla)\text{B}$$$$\Delta\beta=\beta_e-\beta_s\quad\Delta\alpha=\alpha_e-\alpha_s$$$$R_{\text{self}}=(\Delta\alpha-\alpha_e)\nabla\text{M}+(\beta_e-\Delta\beta)\text{D}\nabla^2\text{B}$$$$R_\text{env}=(\Delta\alpha+\alpha_s)\Delta C+(\Delta\beta+\beta_s+\gamma_e\Delta C)\nabla\Phi_Q(x_t,t)$$

ヒントになりそうなのはコイツかなぁってのが：$$\nabla\Phi_Q(x_t,t)=Q_\text{habit}(x_t)+Q_\text{context}(x_t,t)+\epsilon_t$$
$Q_\text{habit}(x_t)$ が慣性由来(圧力の面でいうとこっちが外来種です)、$Q_\text{context}(x_t,t)$ が文脈由来(在来種、本流ね)、$\epsilon_t$ は補正的な引数になるんだけどさぁ.....。
慣性由来が$R_\text{self}$に依るもの・文脈由来が$R_\text{env}$に元から付随してたもの 👈これなら多分何も言われん気がするのね

$Q_\text{habit}(x_t)$ -> $Q_\text{h}(x_t)$ 、$Q_\text{context}(x_t,t)$ -> $Q_\text{c}(x_t,t)$ と略すことにしますわ一旦。
どう含めるかっていうと：$$R_{\text{self}}=(\Delta\alpha-\alpha_e)\nabla\text{M}+((\beta_e-\Delta\beta)\text{D}\nabla^2\text{B})Q_\text{h}(x_t)$$$$R_\text{env}=(\Delta\alpha+\alpha_s)\Delta C+(\Delta\beta+\beta_s+\gamma_e\Delta C)Q_\text{c}(x_t,t)$$
これだと「環境由来の質」と「自力でやった質」とがあるっていう状態になるのね。多分全体の換算で考えると $\epsilon_t$ は必須になるから、一旦質についてはこれで良さげ($\epsilon_t$ は考慮に入れんで良い、補正だからね)としまして。

今回はナビエ-ストークス方程式を基準として作ってみたんだけど、前提条件を踏まえると多分ナビエで合ってるんやろなぁ的な？

多分これならある程度を考慮できるんじゃね？知らんけどさ。一旦GPTにこれを考慮した上での検証してもらうわ(プレプリント論文形式で出力もしてもらいます)
おそらく今日である一定まではいけるかと思われ。

ちなもう就活再開しても良さげです(何なら全振りします、いけるんでね。間に合うから。大丈夫。完全に全振りします、というかできるすね。)
ある程度まとまったんで、あとは俺自身がどれだけ説明できるかなだけですねー、はい。

なおGPTに全部解析してもらった結果をここに出力して頂きました⬇️
[[260925_GBMCCQSD_v1.0-R_決定事項]]

多分ガチで終わりです。一旦凍結なんでね。

一応こっちに書いてないやつのまとめね。
$$\mathcal{B}_t=(\mathcal{Q}_t^{queue},\mathcal{S}_t^{stack})$$
- Queue：未確定候補
- Stack：rollback可能な履歴・snapshot

んでStackとQueueを入れるためにこういう形にする：$$b_t=\psi(\mathcal{B}_t)$$
- $\mathcal{B}_t$：実装上のBuffer
- $b_t$：力学系・数値モデルで扱う状態
- $\psi$：Bufferを数値状態へ変換する写像

んで責任はこうしましょうってことで：$$q_t=w_t\hat{q}_t$$
- $\hat{q}_t$：責任の方向
- $w_t$：責任の重み

んでおそらく実験というか実践用はこう：$$q_t^{meta}=(w_t,\hat{q}_t,\tau_t)$$
んでBufferをこう定義する：$$\Omega_B(t)=\text{現在B内部として扱う範囲}$$$$R_{\mathrm{self}}=\text{現在の }\Omega_B\text{ 内だけで計算可能な寄与}$$
$$R_{\mathrm{context}}=\text{現在の }\Omega_B\text{ 外を参照する寄与}$$

ちな $R_{\mathrm{context}}$ が規模変更で $R_{\mathrm{context}}$ になりました。

Genesisがこれ：$$\Omega_t^+=G_{\Omega}(\Omega_t,\Xi_t)$$
	役割：現在の探索・参照可能範囲を拡張する
Depthがこれ：$$\Omega_{t+1}=D_{\Omega}(\Omega_t^+,q_t)$$
	役割：責任方向、目的、コスト等に応じて最終的な範囲を絞る

なおここまでの骨格として作ってきた標準形：$$G\rightarrow B\rightarrow M\rightarrow C_1\rightarrow C_2\rightarrow Q\rightarrow S\rightarrow D$$
⬆️標準スケジュールとして保存するってことにしますと。

実際に使うのはこういう形：$$\sigma_t=\Gamma(b_t,q_t,\Xi_t)$$$$(\mathcal{B}_{t+1},q_{t+1})=O_{\sigma_t}(\mathcal{B}_t,q_t,\Xi_t)$$
実際の候補こんな感じ：$$\sigma_t\in\{G,M,C_1,C_2,S,D,rollback\}$$
ということで最終形こうなりましたってのが：$$\mathcal{L}_k[b](t)=R_{\mathrm{self}}[b,q;\Omega_t]+R_{\mathrm{context}}[b,q,\Xi;\Omega_t]+U_k(t)$$
- $\mathcal{L}_k$：発展作用素
- $R_{\mathrm{self}}$：内部寄与
- $R_{\mathrm{context}}$：境界外文脈からの寄与
- $U_k$：方程式クラス固有の追加項
$$\lambda_G(t)\mathcal{L}_k[b]=R_{\mathrm{self}}+R_{\mathrm{context}}+U_k$$


んじゃ全部持ってくるすわ。詳しくは上記の資料をご参照くらはい。
$$\mathcal{B}_t=(\mathcal{Q}_t^{queue},\mathcal{S}_t^{stack})\quad b_t=\psi(\mathcal{B}_t)\quad q_t=w_t\hat{q}_t$$$$q_t^{meta}=(w_t,\hat{q}_t,\tau_t)$$
- $\Omega_B(t)$ = 現在 $B$ 内部として扱う範囲
- $R_{\mathrm{self}}$ = 現在の $\Omega_B$ 内だけで計算可能な寄与
- $R_{\mathrm{context}}$ = 現在の $\Omega_B$ 外を参照する寄与 
$$\Omega_t^+=G_{\Omega}(\Omega_t,\Xi_t)\quad\Omega_{t+1}=D_{\Omega}(\Omega_t^+,q_t)$$
$$G\rightarrow B\rightarrow M\rightarrow C_1\rightarrow C_2\rightarrow Q\rightarrow S\rightarrow D$$
$$\sigma_t=\Gamma(b_t,q_t,\Xi_t)\qquad(\mathcal{B}_{t+1},q_{t+1})=O_{\sigma_t}(\mathcal{B}_t,q_t,\Xi_t)$$$$\sigma_t\in\{G,M,C_1,C_2,S,D,rollback\}$$
$$\mathcal{L}_k[b](t)=R_{\mathrm{self}}[b,q;\Omega_t]+R_{\mathrm{context}}[b,q,\Xi;\Omega_t]+U_k(t)$$$$\lambda_G(t)\mathcal{L}_k[b]=R_{\mathrm{self}}+R_{\mathrm{context}}+U_k$$

これで作れるのかどうかだけをちゃんとしておきたいのでもっかい投げます。