#Mine研究/IdeaSketch
bit配列「コレとかコレとか…こういうパターンあるぜ〜！」
-> Assembly「あー、んじゃ…このパターンは『ADD』、このパターンは『MOV』、んでコイツは『JMP』という命令にするすわww」
-> C「いや〜毎回 MOV とか ADD とか JMP 並べんの面倒臭いンゴ…その命令の組み合わせを a+b・if・for・関数・変数っつー“意味のある操作”としてまとめきますかぁ…」
-> 人「c = a + b; でええんか、楽すぎﾜﾛﾀwwwww」

bit列そのものに意味があるわけじゃない 👈これクッソ重要案件かもね

bit列
 ↓ 意味の壁
命令
 ↓ 意味の壁
処理
 ↓ 意味の壁
操作
 ↓ 意味の壁
意図

情報の本体 = bit列、意味 = bit列同士の関係＋読み方

X
├─ 整数として読む
├─ ASCIIとして読む
├─ UTF-8として読む
├─ ARM命令として読む
├─ x86命令として読む
├─ 画像として読む
├─ 音として読む
└─ Mineの4bit状態として読む

Bit Interpreter = Decoder + Consistency

bit列
  ↓
解釈候補を当てる
  ├─ 整数として読む
  ├─ 文字として読む
  ├─ 命令として読む
  ├─ 4bit状態として読む
  └─ 独自ルールで読む
  ↓
それぞれの結果を評価
  ↓
一番整合する解釈を採用
  ↓
mdへ結果・履歴を書く

↑この場合、「Cost $\approx$ 入力bit長 $\times$ 解釈器の数$k$」が通用する

こういう仕様にするか：
- format：type ○○、bit ○○、oprt ○○
- 場の開閉：{} 👈単独でいる場合のみ
- ?! 👈これで問い合わせ、\ / 👈これでコンパイル・コミット

test.md / main.c / Makefile -> minic
- parser= 文法を知る
- mine= 命令の意味を知る
- adder= 回路を知る
- main= 何も知らない

復元可能な情報は書かない。曖昧性は、それが問題になるまで残す。矛盾した時だけ止める。
算術と論理 👈これを無くす必要がある気がするのよな多分。

使用するのは4bit⬇️
- 0000 / 0100 <-> 1000 / 1100
- 0001 / 1001 <-> 0101 / 1101
- 0010 / 1010 <-> 0110 / 1110
- 0011 / 1011 <-> 0111 / 1111

OR  = NOT(NOT A AND NOT B)
XOR = AND / NOT の組み合わせ
ADD = XOR + AND + Carry
SUB = XOR + AND + NOT + Borrow
MUL = AND + Shift + Carry
DIV = AND等から構成した比較・減算・反復

今決めたわ：
- STATE = bitとして机に置くもの
- STACK = 作業中の机
- AND = 共に成立
- NOT = 反転
- ?! = 現状を見る
- \/ = 確定する

これかww：
- main = 起動
- parser = 読む
- loom = 解釈する
- stack = 保留する
- relation = 関係させる
- log = 確定して残す

癖 → 繰り返し現れる関係 → 履歴が必要 → 状態が必要 → 未確定状態をどう持つ？ → STACK → 成立をどう絞る？ → AND → 反転は？ → NOT → 確定したものは？ → LOG

基本の理論はこれでいい：
state・stack・loom(and・not / 階層、最大でも $4^3=64$ までしかないです)
あとは順番なのよ、順不同はマズいんでね。それによって全てが決定されるってのがまだ残ってるんでな。
おそらくそれをやるんがparserとrelationを含めたbufferなのよ。GBMCの機構に対応させればいい説はある。

これよな：
1. set(G)：自己環境 ($\mathbb{S}^1$/ 位相) を基に $S=s_0+s_3I$ を設置。今周の胆力 $g$ が確定
2. hit(B)- /GL >：外部環境 ($\mathbb{S}^1$ / 時間) を参照 ($\mathbb{S}^1\to\mathbb{S}^2$) 後に $M=s_0\hat e$ で変換
3. shoot(M) -> C$_1$ - /DO >：変換後の $\mathbb{S}^2$ に $s_3$・$I$・$\hat f$ の追加 ($\mathbb{S}^2\to\mathbb{S}^2\times\mathbb{S}^2$)。責任の下限 $r_{min}$ ($\mathbb{S}^2$ / 方向性) が確定、最速時間 $\tau_{min}=r_{min}g$ と遊びの幅 $\Delta r=r_{max}-r_{min}$ が定まる
4. C$_2$ -> 責任の矢(Q) - /BO >：遊びの幅の中で $Q$ を探索 ($\mathbb{S}^2\times\mathbb{S}^2\to\mathbb{S}^2)$ 。この探索で $\tau$ を消費する。閾値到達でグレード射影 ($\langle QC\rangle_0$) により責任の実現値 $r$ が確定 ($\mathbb{S}^2\to\mathbb{S}^1$)
5. scalar(S)) -> off(D) - /CM >：実行後、仕事量 $(r-r_{min})g$ を回収し、結果を場の順序で整合。次の胆力 $g$ 、有限性から次周の責任上限 $r_{max}=\tau_{Available}/g$が確定
↑これに対応させれば終了なとこはあるぞ多分w

set(G) -> hit(B) - /GL >  shoot(M) -> pop(vector(C$_1$ - /DO > C$_2$) -> 責任の矢(Q) - /BO > scalar(S)) -> off(D) - /CM > set(G)....
/GL・/DO・/BO・/CM：意味の壁、としましてぇ.... 。
set(G) -> hit(B)・shoot(M) -> pop(vector(C$_1$ - /DO > C$_2$)・scalar(S)) -> off(D)：ここの中身が順不同なはずなんよ。というか螺旋になってるので、割とホロノミー的な感じになると思われる