#KIT/夢考房 
KiCADについて色々やっていきたいと思いまして。調べておりましたけど、大体の流れが分かりましたんで書いておくことにしました。
Skidl ⇨ pcbnew
こうなるらしいです。Skidlってのは回路図を組む用のライブラリ、pcbnewがKiPython用のライブラリなんだけど、SkidlってのはKiPython環境では扱えないライブラリなんすよね。
んでも先に作らなあかんのはSkidlっていうライブラリなので、ローカルである程度回路図作ってから専門的な部品(フットプリントって呼ばれております)を扱うKiPythonに移行する必要がございます。

色々調べた結果、起動するエディタとそれに対応するライブラリがあるっぽいので、対応表だけ下に書いておきます。

| モジュール名   | 役割     | 使う場面        | 拡張子       |
| -------- | ------ | ----------- | --------- |
| skidl    | 回路図生成  | Pythonで回路定義 | .net      |
| pcbnew   | 基板編集   | フットプリント配置   | kicad_pcb |
| eeschema | GUI回路図 | .net読み込み用   | kicad_sch |

#Mine研究 
論理と感覚って話があったじゃん。なんか違和感あんなーって思ってずっと考えてたんだけどさ。
元々は全員論理なのよね。そこである程度勉強すると自分の経験と知識とが結びついてきて、どのジャンルでも似たような構造になってんなーみたいなことに気づくんだけどさ。
これが感覚なのよね。
んでこれ、何かしらの分野を継続している人(動機は何でも良いのよね。モテたいとか憧れの選手んっ美なりたいでも何でも。)はほぼ全員共通認識で持てるんすよね。
しかもこの継続度合いってのもあって、周りが「コイツ狂ってんなwww」という感じでもう笑うしかないみたいな感じになった人に限られるのよね。

パラメータによる気はするね。パラメータが整ってる場合は特に。
全体で揃えるんじゃない、学習教材だけは揃えるんだけど、やり方は各々に任せるべき。

#Mine研究/ワイ理論 
0.5秒ごとに総合活性度を求めた状態で、それをグラフとして出力したら、そのときの空気感の向きというかが分かる気がする。でもそれには責任の矢というベクトルの向きが必要にはなると思うんだけどね。これが意思からの直接送られるデータになるんだけどさ。
以下、プログラムです。

import numpy as np
import matplotlib.pyplot as plt

###### ダミーデータ
t = np.arange(0, 10, 0.5)             # 0～9.5秒
A = np.random.rand(len(t)) * 2        # 活性度サンプル
theta = np.linspace(0, 2*np.pi, len(t))  # 責任の矢の角度サンプル

###### quiver 用にベクトル成分に分解
U = np.cos(theta)  # x成分
V = np.sin(theta)  # y成分

fig, ax = plt.subplots(figsize=(8,4))
###### 矢印を (t, A) の位置にプロット
ax.quiver(t, A, U, V, angles='xy', scale_units='xy', scale=1, width=0.005)
ax.set_xlabel("Time (s)")
ax.set_ylabel("Activation (A = ΔE + ΔE/Δt)")
ax.set_title("空気感の強度と責任の矢の向き")
plt.show()

#KIT/夢考房 
明後日が一応駆動試験なってるんだけどさ。不安ではあるよねw