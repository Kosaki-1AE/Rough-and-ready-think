#Mine研究 
欲しい雰囲気があってなぁ…言語化したいやつがありんしてw
共棲関係・指紋リーダーUSB・FGF4・エレベーター大型犬移動制限・母猫が人間の子供に子猫任せる・山本由伸選手の体の柔らかさ・役割を決めない重要性・Memory Reboot

本当に欲しいのは「C -> Assenbly」の流れなんだわ。

YouTubeなぁ....編集してみるかぁ。ここまで撮って出しだったでしょ。

一応こういう流れかなぁ：
- 横動画撮って、縦動画用として分割処理する(その後縦動画として公開)
- それぞれのタイミングでラベリング対応にしておく、それをJSON形式で保存
- 気づいたことをObsidiianのmdに「#ダンス/YouTube-○○(動画のタイトル)」とかで保存
- 基本JSONの操作はsqlなので....md-jsonがセット、db-pythonでセットって感じかな
- つまり基本動作は「sql + git」っていう作業になるのね多分

これを使うんだろうなぁってのが：
```python
import.Port(x) = (
/* =========================================================
＃       Layer 1 : State
＃       何が変わったか
========================================================= */
	Port(x) = convert(
		Input -> "$delta_z"
		Weight -> "$abln2"
		Memory -> "tau"
	);
	
	Field = (
	    Range    = range(-0.5, 0.5);
	    Boundary = {-0.5, 0.5};
	    Step     = 0.1;
	);
	
	Current.State = Field.state;
	
	Next.State = Current.State + (Port(x) - Current.State) / tau * dt;
	
/* ==========================================================
＃       Layer 2 : Identity
＃       変化前後を同じものとして扱えるか
========================================================= */

	bool Identity;

	if (Field.Boundary.min < Next.State && Next.State < Field.Boundary.max) {
		Identity = 1;
		result   = ACCEPT;
		return 0;
		
	} else if (Next.State == Field.Boundary.min || Next.State == Field.Boundary.max) {
		Identity = 0;
		result   = TRANSITION;
		return 0;
		
	} else {
		result = REJECT;
		return 1;
	}
	
/* =========================================================
＃       Layer 3 : Meaning
＃       そのIdentity判定を、全体としてどう解釈するか
========================================================= */

	Meaning = evaluate(
        Current.State -> Before
        Next.State    -> After
        Identity      -> Identity
        result        -> Result
        Field.Range   -> Range
        Field.Boundary -> Boundary
    );
    Global = (
        Target = Meaning.Target;
        Loss   = loss(Meaning, Target);
    );

    if (result != REJECT && Global.Loss <= Global.Threshold) {
        preserve.Meaning;
        Field.state = Next.State;

    } else if (result != REJECT && Global.Loss > Global.Threshold) {
        reframe.Identity(
            Field.Range,
            Field.Boundary,
            Meaning
        );

    } else {
        reject.Port(x);
        return 1;
    }
    
/* =========================================================
＃       Action
＃       全体整合と個別整合が両方とも同じと見れた場合のみActionになる
========================================================= */

	if (Identity == 1) {
        accept.Port(x);

    } else {
        transition.Port(x);
    }

    continue_action;
    return exit;
}
```

マジでどうしようかなぁ....就活。何もしたいことがないぃぃぃ.....。

ちなTypeScript勉強するんありやなぁと思ってる今日この頃。

#KIT/基本情報技術者試験 
- `arp`：**Address Resolution Protocol**。IPアドレス <-> MACアドレスの接続確認
- AND → 前提条件、OR  → 選択肢(どれでも良いから一つ)、XOR → 二者択一
- 定数 1 (これを基準とするっていう比較判定の基準値のこと)
- 半加算 -> 1の位のみ、全加算 -> 以降全部