#Mine研究 
消す際の1択を何にするか 👈これに全て依存する気がするw
遷移(というかルーティンなのかもしれんが)に身を任せるってのが上級者になるほど勝手に出てきてる気がしてて(ゆーごりんがそうじゃんすか)。
遷移にするだけで「消す際の1択」ってのが確定してくるんすわww


男子のほぼ全員と女子一人(しかも女子の方にはなるべく憧れを持たせた方が良いw)で技術の囲い込みを行う。
男子には普通にエグめのノリを連発、女子には付き合った彼氏の数分だけオブラートを抜いてノリを作っていく。
これで「いじられキャラ」というシステムを作っていくww

策士策に溺れるってのを思い出して思ったんだけどさぁ…。この場合の策士、自分の責任含めてないのよね多分w
なのでここに自分の責任を入れ込むってことをすると…自分の個別整合と場の全体整合ってのを同一視するようになるのよね。
そうすると場の整合でどこに破れがあるかを把握できるようになるんで、まぁ楽になるっていうさ。
この数学版が前に言うた$ab\ln2$のやつですw

同一性の担保をするか否か…らしいわ、どうやら。
要はこういうこと：
- A：a ＝Aの同一性を保持したままのaへの変化
- A -> a ＝Aの同一性を破ってaへ変化する

この状態で初期のプログラムに落としてみるわ。
#Mine研究/実装 
```c
import.Port(x) = (
/* =========================================================
       Layer 1 : State
       何が変わったか
========================================================= */
	Port(x) = convert(
		Input -> "$delta_z"
		Weight -> "$abln2"
		Memory -> "tau"
	);
	
	Field = (
	    Range    = range(-0.5, 0.5);
	    Boundary = {-0.5, 0.5};
	    Step     = 0.1;
	);
	
	Current.State = Field.state;
	
	Next.State = Current.State + (Port(x) - Current.State) / tau * dt;
	
/* ==========================================================
       Layer 2 : Identity
       変化前後を同じものとして扱えるか
========================================================= */

	bool Identity;

	if (Field.Boundary.min < Next.State && Next.State < Field.Boundary.max) {
		Identity = 1;
		result   = ACCEPT;
		return 0;
		
	} else if (Next.State == Field.Boundary.min || Next.State == Field.Boundary.max) {
		Identity = 0;
		result   = TRANSITION;
		return 0;
		
	} else {
		result = REJECT;
		return 1;
	}
	
/* =========================================================
       Layer 3 : Meaning
       そのIdentity判定を、全体としてどう解釈するか
========================================================= */

	Meaning = evaluate(
        Current.State -> Before
        Next.State    -> After
        Identity      -> Identity
        result        -> Result
        Field.Range   -> Range
        Field.Boundary -> Boundary
    );
    Global = (
        Target = Meaning.Target;
        Loss   = loss(Meaning, Target);
    );

    if (result != REJECT && Global.Loss <= Global.Threshold) {
        preserve.Meaning;
        Field.state = Next.State;

    } else if (result != REJECT && Global.Loss > Global.Threshold) {
        reframe.Identity(
            Field.Range,
            Field.Boundary,
            Meaning
        );

    } else {
        reject.Port(x);
        return 1;
    }
    
/* =========================================================
       Action
       全体整合と個別整合が両方とも同じと見れた場合のみActionになる
========================================================= */

	if (Identity == 1) {
        accept.Port(x);

    } else {
        transition.Port(x);
    }

    continue_action;
    return exit;
}
```

駅ってなんか凄い安心感あるよなぁ…。金沢駅着いてそう思ったけど。

車の運転の場合は意思決定過程から権限行使までの速度がほぼ0にならんとダメ
流通のときと自由が効く時とで